{
  "name": "@andrew22lane/bbe",
  "version": "1.2.0",
  "description": "The Brand Builder Engine as one package: the shared static-site engine core, the brand-drift and gate-lint gates, the pack token builder, the byte-identity prover, and `bbe new-surface`, which scaffolds a new site or worker that is clean by construction. Consumers install it and pin a version. Nothing is copied.",
  "type": "module",
  "license": "UNLICENSED",
  "private": false,
  "repository": {
    "type": "git",
    "url": "git+https://github.com/andrew22lane/bbe.git"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "exports": {
    "./engine": "./engine/lib.mjs",
    "./tools/*": "./tools/*"
  },
  "bin": {
    "bbe": "bin/bbe",
    "bbe-new-surface": "bin/bbe-new-surface",
    "bbe-gate": "bin/bbe-gate",
    "bbe-gate-lint": "tools/gate-lint.mjs",
    "bbe-tokens": "tools/build-tokens.mjs"
  },
  "files": [
    "bin/",
    "engine/",
    "tools/",
    "templates/",
    "README.md"
  ],
  "engines": {
    "node": ">=20"
  }
}
